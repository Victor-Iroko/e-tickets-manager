# Deployment used by `npx convex dev`
CONVEX_DEPLOYMENT=

# Convex URLs
CONVEX_URL=
CONVEX_SITE_URL=

# ------------------------------------------------------------------
# VARIABLES BELOW SHOULD BE SET IN CONVEX DASHBOARD (bunx convex env set)
# ------------------------------------------------------------------

# Base URL for Better Auth (matches your Nuxt URL)
# BETTER_AUTH_URL=http://localhost:3000

# Better Auth Secret (generate with: openssl rand -base64 32)
# BETTER_AUTH_SECRET=

# Google OAuth (https://console.cloud.google.com)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# SMTP Configuration for sending password reset emails
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_SECURE=false  # Set to true for SSL (port 465), false for STARTTLS (port 587)
